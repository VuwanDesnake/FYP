import DynamicCalculator from "@/components/DynamicCalculator";

const Transport = () => (
  <DynamicCalculator
    category="transport"
    title="Transport"
    description="Select your mode of transport and enter the distance."
    quantityLabel="Distance"
  />
);

export default Transport;
