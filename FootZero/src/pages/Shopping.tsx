import DynamicCalculator from "@/components/DynamicCalculator";

const Shopping = () => (
  <DynamicCalculator
    category="shopping"
    title="Shopping"
    description="Select a purchase category and quantity."
    quantityLabel="Quantity"
  />
);

export default Shopping;
